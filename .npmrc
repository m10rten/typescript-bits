# Enforce exact versions in package.json
save-exact=true

# Prevent malicious post-install scripts
ignore-scripts=true

# Force developers to use the correct Node/npm versions
engine-strict=true

# Keep the security scanner active
audit=true

# Ensure lockfiles are always generated and updated
package-lock=true